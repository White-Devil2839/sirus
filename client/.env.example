# SIRUS client environment
# In local dev this is unused — Vite proxies /api to the server (see vite.config.js).
# In production (Vercel/Netlify), set this to your deployed API origin, e.g.:
# VITE_API_URL=https://sirus-api.onrender.com/api
VITE_API_URL=http://localhost:5050/api
